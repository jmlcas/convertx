# ConvertX

Puedes cambiar JWT_SECRET con este comando: openssl rand -base64 32

Después del registro puede cambiar a false para impedir nuevos registros  - ACCOUNT_REGISTRATION=false

Ver en "http://localhost:8200"
